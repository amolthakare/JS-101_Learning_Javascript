var storedUsername = "amol";
var storedPassword = 32;

var username = "amol";
var password = 32;

if(storedUsername==username && storedPassword==password){
  console.log("user can login");
}

else{
  console.log("user cannot login");
}